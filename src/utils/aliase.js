import * as PIXI from 'pixi.js'

//Aliases
export const Application = PIXI.Application, // 应用类
  Loader = PIXI.Loader, // 加载器
  Sprite = PIXI.Sprite, // 精灵类
  TextureCache = PIXI.utils.TextureCache, // 纹理缓存
  Rectangle = PIXI.Rectangle,
  Graphics = PIXI.Graphics, // 绘图
  Text = PIXI.Text, // 文本
  AnimatedSprite = PIXI.AnimatedSprite // 动画精灵

  console.log(PIXI)
